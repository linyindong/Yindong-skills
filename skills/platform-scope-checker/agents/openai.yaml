interface:
  display_name: "Platform Scope Checker"
  short_description: "Gate MVP scope, hidden complexity, and build risk"
  default_prompt: "Use $platform-scope-checker to decide whether this change belongs now, should be simplified, validated first, or deferred."
